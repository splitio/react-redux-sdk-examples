// Your SDK settings for browser.
// Replace authorizationKey with your client-side SDK key.
export const config = {
  core: {
    authorizationKey: '<your-sdk-key>',
    key: '<customer-key>'
  }
}

// Replace the following with the name of the feature flag you want to evaluate.
export const featureFlagName = 'test_feature_flag';
